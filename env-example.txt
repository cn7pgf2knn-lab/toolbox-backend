# Database (wordt automatisch ingevuld door Render)
DATABASE_URL=postgresql://user:password@host:5432/database

# Secret key voor JWT tokens (wordt automatisch ingevuld door Render)
SECRET_KEY=your-secret-key-here
